<h1>Sign-up Form</h1>

<%= form_for(:user, :url => {:action => 'create'}) do |f|%>
	
	<table>
		<tr>
			<th> Name </th>
			<td><%= f.text_field(:first_name)%></td>
		</tr>
	</table>
	
<%= f.submit %>
<% end %>			